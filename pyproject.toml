[project]
name = "smart-fut"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.136.3",
]

[dependency-groups]
dev = [
    "fastapi>=0.136.3",
    "pytest>=9.0.3",
    "ruff>=0.15.14",
]
